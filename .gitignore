.venv/
__pycache__/
*.pyc
.ipynb_checkpoints/
.DS_Store

# Данные соревнования закрыты — не коммитим raw csv
data/raw/*.csv

# Промежуточные артефакты обучения (большие, регенерируются)
artifacts/
.cache/

# Submissions — оставляем только итог; промежуточные не пушим
submissions/*.csv
!submissions/.gitkeep
